# 复制成 .env 后按需修改（.env 已被 git 忽略，不会冲突、不会上传）
#
# 宿主机(NAS)对外端口。冲突就改这个（比如 8000 被系统占用）
STOCK_PORT=8000

# 数据目录。默认是项目下的 ./data；
# 想让数据放到 NAS 的固定目录，就写绝对路径，例如：
# STOCK_DATA=/vol1/1000/docker/personal-stock/data
STOCK_DATA=./data
